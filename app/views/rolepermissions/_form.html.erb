<%= form_with(model: rolepermission, local: true) do |form| %>
  <% if rolepermission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rolepermission.errors.count, "error") %> prohibited this rolepermission from being saved:</h2>

      <ul>
      <% rolepermission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
