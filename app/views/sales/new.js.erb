var products = document.getElementById('products')
while (products.firstChild) {
    products.removeChild(products.firstChild);
  }
products.insertAdjacentHTML('afterbegin', '<%=j render "purshas/card_products", category: Category.find(params[:category_id])%>');
