<%= form_with(model: payment, local: true) do |form| %>
  <% if payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :discountPer %>
    <%= form.text_field :discountPer %>
  </div>

  <div class="field">
    <%= form.label :discountVal %>
    <%= form.text_field :discountVal %>
  </div>

  <div class="field">
    <%= form.label :payment %>
    <%= form.text_field :payment %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="field">
    <%= form.label :transid %>
    <%= form.text_field :transid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
